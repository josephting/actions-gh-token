name: pipeline
on:
  pull_request: {}

permissions:
  id-token: write

jobs:
  test:
    name: Github Token
    runs-on: ubuntu-latest
    steps:
      - name: Print env
        run: env | grep -i token
